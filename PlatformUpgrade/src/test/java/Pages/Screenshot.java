package Pages;

public class Screenshot {

    /*
        //
//    String imgOriginal = "C:\\comparescreenshots\\dev.jpg";
//    String imgCompared = "C:\\comparescreenshots\\prod.jpg";
    String imgOutputDiff = "C:\\DifferencesScreesnots\\"+(getCurrUrl().lastIndexOf("/"))+ ".jpg";

//
//    File devfile;
//    File prodfile;

        devfile = ((TakesScreenshot) WebConnector.currentDriver()).getScreenshotAs(OutputType.FILE);
// Now you can do whatever you need to do with it, for example copy somewhere
//        FileUtils.copyFile(scrFile, new File("C:\\comparescreenshots\\dev.png"));
/        prodfile = ((TakesScreenshot) WebConnector.currentDriver()).getScreenshotAs(OutputType.FILE);
// Now you can do whatever you need to do with it, for example copy somewhere
//        FileUtils.copyFile(scrFile, new File("C:\\comparescreenshots\\prod.png"))

        ImageComparison imageComparison = new ImageComparison(10, 10, 0.05);
//        try {
//            if(imageComparison.fuzzyEqual(imgOriginal,imgCompared,imgOutputDiff)){
//                System.out.println("Images are same");
//            }else{
//                System.out.println("Images are not fuzzy-equal");
//            }
//        } catch (IOException e) {
//            e.printStackTrace();
//        }
        System.out.println("The output path is ***********************************************"+imgOutputDiff);
//        Assert.assertTrue(imageComparison.fuzzyEqual(devfile, prodfile, imgOutputDiff));
//        Assert.assertTrue(imageComparison.fuzzyEqual(imgOriginal, imgCompared, imgOutputDiff));




     */
}
